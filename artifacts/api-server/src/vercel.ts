// Serverless entry (Vercel). Exports the Express app instead of listening on a
// port; api/index.js at the repository root forwards each request to it.
import app from "./app";

export default app;
